{
  "name": "adm-portal",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "export APP_PATH=${APP_PATH:=$PWD/app} && export LOG_PATH=${LOG_PATH:=$PWD/logs} && nodemon"
  },
  "dependencies": {
    "@clich/webpack-bnp": "^1.0.0"
  }
}
